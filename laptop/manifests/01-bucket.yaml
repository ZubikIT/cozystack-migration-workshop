# Namespace tenant-workshop80 уже подставлен — менять ничего не нужно.
# Проще разом по всем файлам: см. README, раздел про подстановку номера.
#
apiVersion: apps.cozystack.io/v1alpha1
kind: Bucket
metadata:
  name: my-images
  namespace: tenant-workshop80
spec:
  users:
    app: {}