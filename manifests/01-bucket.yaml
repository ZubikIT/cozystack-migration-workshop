# Перед применением замените tenant-pXX на свой namespace.
# Проще разом по всем файлам: см. README, раздел про подстановку номера.
#
apiVersion: apps.cozystack.io/v1alpha1
kind: Bucket
metadata:
  name: my-images
  namespace: tenant-pXX
spec:
  users:
    app: {}